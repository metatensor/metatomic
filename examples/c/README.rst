.. _c-tutorials:

C API tutorials
===============

Create a :c:type:`mta_system_t` from existing arrays, register a shifted
Lennard-Jones :c:type:`mta_model_t`, load it through a plugin, and use the
string / error / unit helpers.